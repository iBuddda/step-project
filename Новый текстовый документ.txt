в переменную счетчик поместить количество символов
создать текстову.ю переменную
начать обратный цикл с декрементацией
в текстовую переменную поместить еЄ1 же зщначение плюс символ из индекса счетчика


int aFF = mail->Length;
string^ bFF = "";
array<Char> dFF = mail->ToCharArray();
while (aFF > 0) {

	bFF = bFF + dFF[aFF];
	aFF--;
}
mail = bFF;

array<Char>(4,7) 
int a = 0;
while (a < 3)
{

	a++;
}


bool update_array(direction:string);
{
		int a = G“екуща€ѕоизци€ячейкийц¬ыбрванной;
	if (direction == 'yp')
	{
		array1[a-1,2]:=array1[a,2];
	}
		if (direction == 'ym')
	{
		array1[a+1,2]:=array1[a,2];
	}
		if (direction == 'yl')
	{
		array1[a,2-1]:=array1[a,2];
	}
		if (direction == 'yl')
	{
		array1[a,2+1]:=array1[a,2];
	}
}



1 разместить пл€ ввода
		а - навзание товара
		б - цена товара
		ц - количество на складе
		д - ид товара
		
2 создать переменные (глоабльные)
		помещ€ть значени€ полей при
		изменении в глобальные переменные
		
		(об€зтаельное тербование)
		перемнные наход€тс€ в отдельном файле
		дл€ инклуда 
		
3 создать функцию, котора€ сохран€ет
  зеначение в файле, добавл€€ в конец строки,
  файл можно перезаписывать с полной отчисткой,
  текст должен быть доступен дл€ распарсинга,
  с использованием ключевых слов, дл€ определени€
  к какому полю принадлежит значение
  
4 созщдать функцию дл€ загрузки файла, и распарсинга
  файла построчно, помещ€ть это значение в поле ввода
  











